# 🔱 FALLBACK SOLUTION: ASYNC-FIRST APPROACH
# Use if Supabase 2.7.0 downgrade fails

# Core Dependencies
python-telegram-bot==20.6
openai==1.97.0
httpx==0.25.2

# Alternative Supabase approach - async client only
postgrest-py==0.13.0      # Direct PostgREST client
gotrue-py==1.3.0          # Supabase auth
realtime-py==0.1.5        # Realtime subscriptions

# Database alternative
asyncpg==0.29.0           # Direct PostgreSQL async client
sqlalchemy==2.0.23        # ORM alternative

# Supporting libraries
python-dotenv==1.0.0
pydantic==2.5.0
structlog==23.2.0