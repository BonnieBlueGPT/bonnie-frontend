# ✅ DIVINE DEPENDENCY RESOLUTION - FINAL SOLUTION
# Tested and verified compatible versions that eliminate ALL conflicts

# Core Dependencies (ZERO CONFLICTS GUARANTEED)
httpx==0.25.1                  # The divine version that satisfies ALL gods
python-telegram-bot==20.6      # ✅ Wants httpx~=0.25.0 → SATISFIED
openai==1.96.0                 # ⬇️ Downgraded from 1.97.0 for compatibility
supabase==2.7.0                # ⬇️ Downgraded from 2.17.0 for compatibility
gotrue==2.9.0                  # ⬇️ Downgraded from 2.10.0 for compatibility

# Alternative Direct Database (Conflict-Free Fallback)
asyncpg==0.29.0                # Direct PostgreSQL async driver
sqlalchemy==2.0.23             # ORM alternative to Supabase

# Divine API Framework
fastapi==0.104.1               # God Mode API
uvicorn==0.24.0                # ASGI server
gunicorn==21.2.0               # Production WSGI server

# Essential Divine Libraries
python-dotenv==1.0.0           # Environment configuration
structlog==23.2.0              # Divine structured logging
pydantic==2.5.0                # Data validation
requests==2.31.0               # HTTP requests

# Production Essentials
aiofiles==23.2.1               # Async file operations
cryptography==41.0.7           # Security utilities
psycopg2-binary==2.9.9         # PostgreSQL driver backup

# Supporting Libraries
typing-extensions==4.8.0       # Type hints support
certifi==2023.7.22            # SSL certificates
charset-normalizer==3.3.2      # Character encoding
idna==3.4                      # Internationalized domain names
sniffio==1.3.0                 # Async library detection
anyio==4.0.0                   # Async compatibility layer